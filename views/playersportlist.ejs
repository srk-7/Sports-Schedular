<h5>
    <%= title %> <span
    class="bg-gray-100 text-gray-800 text-sm font-small mr-2 px-2.5 py-0.5 rounded dark:bg-gray-200 dark:text-gray-900 justify-center items-center items">
    <%= data.length %>
</span>
</h5>
<ul class="list-none">
    <h2>THE LIST OF SPORTS THAT ARE AVAILABLE:</h2>
    <% for(var i=0;i<data.length;i++)
    {%>
        <li class="Todo-Item">
            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
                    <a href="sports/<%= data[i].id %>" onclick="<%= data[i].name %>"><%= data[i].name %></a> 
            </div>
        </li>
    <% } %>
</ul>